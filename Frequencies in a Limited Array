Frequencies in a Limited Array

https://www.geeksforgeeks.org/problems/frequency-of-array-elements-1587115620/1

class Solution:
    def frequencyCount(self, arr):
        n = len(arr)
        freq = [0] * n  # create result array of size n
        
        for num in arr:
            freq[num - 1] += 1  # shift index because of 1-based values
        
        return freq


==============================OR==========================
using counter 

from collections import Counter

class Solution:
    def frequencyCount(self, arr):
        n = len(arr)
        count = Counter(arr)   # Count each element's occurrences
        freq = [0] * n         # Result array of size n

        for i in range(1, n+1):
            freq[i-1] = count.get(i, 0)  # Get frequency of number i, default 0
        
        return freq
